"""Console-script entry points for FairPrice-MF.

These are wired up via `[project.scripts]` in `pyproject.toml`:

- ``fairprice-audit``  → :func:`fairprice_mf.scripts.audit.main`
- ``fairprice-serve``  → :func:`fairprice_mf.scripts.serve.main`
"""
